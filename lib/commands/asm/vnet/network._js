/**
 * Copyright (c) Microsoft.  All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// Warning: This code was generated by a tool.
// 
// Changes to this file may cause incorrect behavior and will be lost if the
// code is regenerated.

/*

Generated Command List:

azure network abort-migration 
--virtual-network-name $p0

azure network commit-migration 
--virtual-network-name $p0

azure network prepare-migration 
--virtual-network-name $p0


*/

'use strict';
var profile = require('../../../util/profile');
var utils = require('../../../util/utils');

var $ = utils.getLocaleString;exports.init = function (cli) {

/*
  Network AbortMigration
  --virtual-network-name
*/
  var networkAbortMigration = cli.category('invoke').description('Commands to invoke service management operations.').category('network')
  .description($('Commands to manage your network.  '));
  networkAbortMigration.command('abort-migration [virtual-network-name]')
  .description($('Abort Virtual Network migration api validates and aborts the given virtual network for IaaS Classic to ARM migration.'))
  .usage('[options] <virtual-network-name>')
  .option('--virtual-network-name <virtual-network-name>', $('virtual-network-name'))
  .option('-s, --subscription <subscription>', $('The subscription identifier'))
  .execute(function(virtualNetworkName, options, _) {
    if (!virtualNetworkName) {
      virtualNetworkName = cli.interaction.promptIfNotGiven($('virtual-network-name : '), virtualNetworkName, _);
    }

    cli.output.verbose('virtualNetworkName = ' + virtualNetworkName);
    var subscription = profile.current.getSubscription(options.subscription);
    var networkManagementClient = utils.createNetworkClient(subscription);
    var result = networkManagementClient.network.abortMigration(virtualNetworkName, _);
    if (result) {
      cli.output.json(result);
    }
  });
/*
  Network CommitMigration
  --virtual-network-name
*/
  var networkCommitMigration = cli.category('invoke').description('Commands to invoke service management operations.').category('network')
  .description($('Commands to manage your network.  '));
  networkCommitMigration.command('commit-migration [virtual-network-name]')
  .description($('Commit Virtual Network migration api validates and commits the given virtual network for IaaS Classic to ARM migration.'))
  .usage('[options] <virtual-network-name>')
  .option('--virtual-network-name <virtual-network-name>', $('virtual-network-name'))
  .option('-s, --subscription <subscription>', $('The subscription identifier'))
  .execute(function(virtualNetworkName, options, _) {
    if (!virtualNetworkName) {
      virtualNetworkName = cli.interaction.promptIfNotGiven($('virtual-network-name : '), virtualNetworkName, _);
    }

    cli.output.verbose('virtualNetworkName = ' + virtualNetworkName);
    var subscription = profile.current.getSubscription(options.subscription);
    var networkManagementClient = utils.createNetworkClient(subscription);
    var result = networkManagementClient.network.commitMigration(virtualNetworkName, _);
    if (result) {
      cli.output.json(result);
    }
  });
/*
  Network PrepareMigration
  --virtual-network-name
*/
  var networkPrepareMigration = cli.category('invoke').description('Commands to invoke service management operations.').category('network')
  .description($('Commands to manage your network.  '));
  networkPrepareMigration.command('prepare-migration [virtual-network-name]')
  .description($('Prepare Virtual Network migration api validates and prepare the given virtual network for IaaS Classic to ARM migration.'))
  .usage('[options] <virtual-network-name>')
  .option('--virtual-network-name <virtual-network-name>', $('virtual-network-name'))
  .option('-s, --subscription <subscription>', $('The subscription identifier'))
  .execute(function(virtualNetworkName, options, _) {
    if (!virtualNetworkName) {
      virtualNetworkName = cli.interaction.promptIfNotGiven($('virtual-network-name : '), virtualNetworkName, _);
    }

    cli.output.verbose('virtualNetworkName = ' + virtualNetworkName);
    var subscription = profile.current.getSubscription(options.subscription);
    var networkManagementClient = utils.createNetworkClient(subscription);
    var result = networkManagementClient.network.prepareMigration(virtualNetworkName, _);
    if (result) {
      cli.output.json(result);
    }
  });


};
